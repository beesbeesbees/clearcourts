<div class="container">
  <div class="row">
    <div class="row">
      <div class="col-sm-8 col-sm-offset-2">
        <div class="light well">
          <table class="table">
            <thead>
              <tr>
                <th>User</th>
                <th>Court</th>
                <th>Logins</th>
                <th>Last Activity</th>
              </tr>
            </thead>
            <tbody>
            <tr>
              <% @users.each do |user| %>
                <td><%= link_to user.email, [:admin, user] %></td>
                <td><%= user.court || "No Court Associated" %></td>
                <td><%= user.sign_in_count %></td>
                <td><%= [time_ago_in_words(user.last_sign_in_at), " ago"].join if user.sign_in_count > 0 %></td>
              </tr>
              <% end %>
            </tbody>
          </table>
          <%= link_to "Add User", new_admin_user_path, class: "btn btn-default" %>
        </div>
      </div>
    </div>
  </div>
</div>
